import {Button, StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 30,
        alignItems: 'center'
    },

    title: {
        fontWeight: 'bold',
        marginBottom: 20,
        fontSize: 18
    },

    info: {
        marginBottom: 20,
        fontSize: 18
    },

    button: {
        padding: 10,
        borderWidht: 1,
        borderColor: '#F60',
        borderRadius: 10,
        width: '100%'
    },

    textButton: {
        fontSize: 18,
        textAlign: 'center',
        color: '#F60',
    }
})

export default styles